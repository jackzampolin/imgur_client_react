React Imgur Client with ReactRouter
====

Checkout this repo, install depdencies, then start the gulp process with the following:

```
	> git clone git@github.com:StephenGrider/ReactStarter.git
	> cd imgur_react_client
	> npm install
	> gulp
```
